# Чек-лист сдачи решения

Собран из требований [ТЗ](01-task-statement.md), [технических требований](05-technical-requirements.md) и [критериев оценки](06-evaluation-criteria.md). Рядом с каждым пунктом — раздел критериев, на который он влияет.

Статус проставлен 2026-09-05 по строгой сверке репозитория с ТЗ (курсивом — где именно это сделано или что не хватает).

## Обязательный функционал

- [x] Веб-сервис: выбор региона на карте — *Р2 Управление полигонами, Р5 Удобство* — *карта AntV L7 на тайлах OpenStreetMap, любая точка мира (`web/src/components/map/FieldMap.tsx`)*
- [x] Веб-сервис: автоматический поиск готовых с/х контуров для региона (OSM / WorldCereal) — *Р2 Управление полигонами* — *OSM Overpass `landuse=farmland` (`GET /api/fields`); WorldCereal не подключён*
- [x] Веб-сервис: рисование произвольного полигона на карте — *Р2 Управление полигонами* — *`@antv/l7-draw`*
- [x] Управление набором полигонов: добавить, открыть без повторного сбора, удалить — *Р2 Управление полигонами* — *`service/polygons.py`, `/api/user-polygons`, карточка «Мои поля»*
- [x] Автосбор спутниковых данных по полигону (Sentinel-2 / Landsat / MODIS) без ручной загрузки датасета — *Р2 Автосбор* — *`service/collect.py`: Planetary Computer, запасной Earth Search*
- [x] Автосбор метеоданных (ERA5 / ERA5-Land) — *Р2 Автосбор* — *Open-Meteo ERA5 по центроиду*
- [x] Корректная работа, когда часть источников недоступна — *Р2 Автосбор* — *каждый источник в try/except, в интерфейсе выводится, что собрано (exp-104)*
- [x] Формирование временного ряда NDVI, очистка, восстановление пропусков — *Р0 Метрика* — *приоритет S2 → Landsat → MODIS, артефакты, гармонизированная ежедневная кривая; для test — LightGBM + SeasonNet*
- [x] Отображение исходного и восстановленного ряда `primary_ndvi` с визуальным различием точек — *Р5 Удобство* — *точки по сенсорам, чёрная кривая, ромбы контрольных точек*
- [x] Детекция негативных аномальных периодов — *Р2 Детекция аномалий* — *`anomaly/detect.py`*
- [x] Интерпретация аномалий (климатическая норма, погода, несколько источников) — *Р2 Детекция аномалий* — *`anomaly/report.py`: норма, ERA5, фенология, региональный контекст, NDWI*
- [x] Отображение аномалий на карте и графиках — *Р5 Удобство* — *полосы эпизодов на графиках NDVI и отклонения, отметки лет в переключателе, цвет контура на карте по состоянию последнего сезона*
- [x] Batch-инференс: `private_features.csv` → `submission.csv` только для `is_synthetic_gap = True` — *Р0 Метрика* — *`gapfill.predict_saved` (без обучения) и `gapfill.predict`*
- [x] Решение не привязано к одному региону / заранее заданному набору территорий — *Р2 Адаптивность* — *STAC/ERA5/OSM глобальны; норма строится по истории самого поля*

## `submission.csv`

- [x] Колонки ровно: `anon_polygon_id,date,primary_ndvi_pred`
- [x] Только строки с `is_synthetic_gap = True` — *2 323 строки для второй версии test*
- [x] Каждая пара `anon_polygon_id + date` ровно один раз — *проверяется в `write_submission`*
- [x] Нет `NaN`, пропусков и лишних строк — *проверяется в `write_submission`*
- [x] Дата в формате `YYYY-MM-DD`, разделитель — запятая, кодировка UTF-8
- [x] Локальная валидация на synthetic gaps из train даёт RMSE заметно ниже 0.10 — *0.055 (baseline 0.093), см. exp-007*

## Репозиторий и README — *Р4 Документация (0–5)*

- [x] Непустой `README.md` в корне
- [x] Назначение решения и структура репозитория
- [x] Стек технологий
- [x] Подготовка окружения и установка зависимостей — *uv, группы зависимостей*
- [x] Источники и структура данных — *README + docs/03*
- [x] Команды сбора / предобработки данных — *сбор — сервисом (`POST /api/analyze`); предобработка внутри `gapfill.data`*
- [x] Команда обучения или порядок подключения готовых артефактов — *`models/` + `gapfill.predict_saved`*
- [x] Команда инференса и путь к `submission.csv`
- [x] Команда запуска веб-сервиса и детекции аномалий
- [x] Настройки внешних API (ключи, переменные окружения, где взять доступ) — *ключи не нужны; `ANTHROPIC_API_KEY` опционально*
- [x] Ожидаемые входные / выходные файлы
- [x] Все внешние данные и предобученные модели перечислены — *внешних обучающих данных нет; проверенные готовые модели — exp-006*

## Окружение и артефакты

- [x] Версии зависимостей зафиксированы (`pyproject.toml` + lock / `requirements.txt` / `environment.yml`) — *`pyproject.toml` + `uv.lock`*
- [x] Dockerfile / docker-compose (предпочтительно) — *`docker compose up --build` собран и проверен: интерфейс и API отвечают, batch-инференс внутри контейнера воспроизводит `submission.csv` (расхождение 7e-5 из-за CPU против GPU)*
- [x] Системные зависимости перечислены — *раздел «Зависимости» README: только Python 3.14 + uv; GDAL/PROJ идут в wheels; GPU не обязателен*
- [x] Random seed и ключевые гиперпараметры зафиксированы — *seed 42, параметры в `gapfill/train.py`, `anomaly/config.py`, docs/12*
- [x] Веса, скейлеры, статистики климатологии и прочие артефакты доступны и подключаются по инструкции — *`models/` (~60 МБ) в репозитории; норма считается из данных*
- [x] Большие файлы — по внешней ссылке из README — *не требуется, всё в репозитории*

## Код — *Р4 Качество кода (0–3)*

- [x] Понятные точки входа, конфигурации и модули — *`gapfill/`, `anomaly/`, `service/`, конфиги `*/config.py`*
- [x] Комментарии **на русском языке**, по делу
- [x] Нет дублирования и хаотично размещённой логики — *функции до 50 строк, файлы до 400 строк; 17 тестов*

## Исследовательский отчёт — *Р3 (0–10)*

- [x] Описание задачи и протокола оценки — *docs/14 §1*
- [x] EDA: структура рядов, пропуски, сезонность, выбросы, различия между полигонами/регионами — *docs/08, docs/11*
- [x] Использованные данные: что, откуда, почему — *docs/14 §3*
- [x] Baseline: назначение, компоненты, сильные/слабые стороны, выявленные ограничения, гипотезы улучшений — *docs/14 §4*
- [x] Эксперименты: условия, сравнение вариантов, абляции, объяснение эффекта — *experiments/exp-000…007, 100…104*
- [x] Что дало / не дало прирост качества интерполяции — *docs/12, docs/14 §5*
- [x] Логика детекции аномалий и примеры найденных случаев — *docs/13*
- [x] Обоснование финальной конфигурации — *docs/14 §7*
- [x] Ограничения, проблемные случаи, направления развития — *docs/14 §8*
- [x] Инструкции и параметры воспроизводимости — *docs/14 §9, README*
- [x] Каждый график сопровождается выводом — *docs/08, docs/13*

## Презентация — *Р1 (0–15)*

- [ ] Проблематика, данные, подход, работающий результат — *не начата*
- [ ] Baseline и улучшение относительно него
- [ ] Итоговый GapScore / RMSE
- [ ] Логика детекции аномалий
- [ ] 2–3 показательных временных ряда — *кандидаты: AOI-0065 2024 (засуха), AOI-0043 2019 (не засеяно), AOI-0021 2017 (севооборот), см. `reports/anomalies/figures/`*
- [ ] Архитектура / пайплайн
- [ ] Скриншоты и живая демонстрация интерфейса, основной пользовательский путь
- [ ] Выводы по ограничениям и развитию
- [ ] Единая цветовая схема и типографика, без длинных текстовых блоков, без орфографических ошибок
- [ ] Готовы ответить: происхождение обучающих данных, выбор методов, ключевые эксперименты, поведение на новых регионах — *ответы собраны в docs/14 и docs/15*

## Не сделано или не проверено (на 2026-09-05)

1. Презентация — не начата (15 баллов).
2. Объяснения языковой моделью (`anomaly/llm.py`) не проверены: нет ключа `ANTHROPIC_API_KEY`.
3. Самопроверка по опубликованным ответам к первой версии test — ждём файл ответов.
4. WorldCereal как второй источник готовых контуров не подключён (только OSM).
5. Тайлы карты берутся напрямую с `tile.openstreetmap.org`; для публичного развёртывания нужен свой кэш.
