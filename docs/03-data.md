# Данные соревнования и формат submission

> Источник: «Космохакатон — Постановка задачи» ([PDF](source/postanovka-zadachi.pdf)), стр. 11–13.

## Общее описание

Участникам с начала хакатона предоставляются два набора данных:

| Файл | Назначение |
|---|---|
| `train_dataset.csv` | Обучающий (reference) набор |
| `private_features.csv` | Тестовый набор |

Данные представлены в табличном виде: **одна строка = один анонимизированный полигон в одну дату**. Строка однозначно идентифицируется комбинацией `anon_polygon_id + date`.

### Состав признаков

| Группа | Признаки |
|---|---|
| Идентификаторы | `anon_polygon_id`, `date` |
| Спутниковые индексы | Sentinel-2, Landsat, MODIS |
| Метеоданные | ERA5 |
| Календарные признаки | `year`, day-of-year |
| Целевая переменная | `primary_ndvi` |
| Климатическая норма | характеристики климатической нормы, `n_reference_years` |
| Служебные | `is_synthetic_gap` |
| Статичные | `crop_type` |

В `train_dataset.csv` целевые значения `primary_ndvi` известны. В `private_features.csv` для контрольных строк с `is_synthetic_gap = True` значение `primary_ndvi` скрывается, а динамические и вычисляемые признаки этой строки маскируются.

## Train (обучающий датасет)

Участникам предоставляется reference-набор `train_dataset.csv`:

- **99 955 строк** с известным `primary_ndvi` и сопутствующими признаками;
- дополнительно содержит `ndvi_zscore` и `status`, **которых не будет в test**.

Его можно использовать для построения baseline, обучения и проверки пайплайна. При необходимости команды могут расширять обучающую выборку за счёт открытых источников.

### Рекомендации по использованию и расширению train

- использовать доступные контуры сельскохозяйственных полей из OpenStreetMap / Overpass API, ESA WorldCereal либо собственные полигоны;
- собирать дополнительные временные ряды Sentinel-2, Landsat и MODIS по выбранным полигонам;
- добавлять погодные признаки ERA5 / ERA5-Land или данные других открытых метеоисточников;
- самостоятельно создавать валидационные synthetic gaps из известных точек, чтобы сравнивать методы восстановления;
- фиксировать источники данных, параметры фильтрации облаков, агрегации и все этапы предобработки для воспроизводимости.

Допускается использование других открытых источников и публичных предобученных моделей, если это не нарушает их лицензии и правила соревнования. **Все внешние данные и модели должны быть перечислены и описаны в отчёте и README.**

## Test (тестовый датасет)

Участникам с начала хакатона предоставляется тестовый датасет `private_features.csv`.

- Для контрольных точек значение `primary_ndvi` скрывается, строка помечается `is_synthetic_gap = True`, а остальные динамические и вычисляемые признаки в этой строке также маскируются.
- Участник должен восстановить `primary_ndvi` **только для строк с `is_synthetic_gap = True`**.
- Правильные значения `primary_ndvi` для контрольных точек участникам не предоставляются. Ground truth хранится у организаторов и используется только для автоматической оценки решений на платформе.

### Что оценивается, а что нет

- Для проверки задачи 1 команда формирует `submission.csv` с предсказанными значениями `primary_ndvi` для всех контрольных точек и загружает его на платформу. Платформа автоматически сопоставляет предсказания со скрытыми правильными ответами и рассчитывает итоговые RMSE и GapScore.
- **Естественные пропуски**, возникшие из-за отсутствия пригодного спутникового наблюдения, сами по себе **не являются контрольными точками** автоматической метрики.
- Для задачи 1 оцениваются только строки, специально помеченные `is_synthetic_gap = True`.

## Формат `submission.csv`

Файл формируется **только для строк, где `is_synthetic_gap = True`**, и должен содержать три колонки:

| Колонка | Описание |
|---|---|
| `anon_polygon_id` | Анонимный идентификатор полигона |
| `date` | Дата контрольной точки в формате `YYYY-MM-DD` |
| `primary_ndvi_pred` | Восстановленное значение `primary_ndvi` (вещественное число) |

Требования к файлу:

- пара `anon_polygon_id + date` должна встречаться в submission **ровно один раз** и соответствовать контрольной строке тестового набора;
- пропуски, `NaN` и дополнительные строки **не допускаются**;
- разделитель CSV — запятая;
- кодировка — UTF-8.

### Пример

```csv
anon_polygon_id,date,primary_ndvi_pred
AOI-0001,2025-06-14,0.543218
AOI-0037,2025-07-03,0.391005
```

## Фактическая структура датасетов в репозитории

Датасеты лежат в папке [`data/`](../data/):

| Файл в репозитории | Название в ТЗ | Назначение |
|---|---|---|
| `data/train_dataset.csv` | `train_dataset.csv` | Обучающий (reference) набор |
| `data/test_dataset.csv` | `private_features.csv` | Тестовый набор — **это один и тот же файл**, просто в репозитории он назван иначе |

### Колонки

| Колонка | Описание | Train | Test |
|---|---|---|---|
| `anon_polygon_id` | Анонимный идентификатор полигона (`AOI-0001` … `AOI-0078`) | ✔ | ✔ |
| `date` | Дата наблюдения, `YYYY-MM-DD` | ✔ | ✔ |
| `s2_ndvi` | NDVI по Sentinel-2 | ✔ | ✔ |
| `s2_evi` | EVI по Sentinel-2 | ✔ | ✔ |
| `s2_ndwi` | NDWI по Sentinel-2 | ✔ | ✔ |
| `landsat_ndvi` | NDVI по Landsat | ✔ | ✔ |
| `landsat_evi` | EVI по Landsat | ✔ | ✔ |
| `landsat_ndwi` | NDWI по Landsat | ✔ | ✔ |
| `modis_ndvi` | NDVI по MODIS | ✔ | ✔ |
| `modis_evi` | EVI по MODIS | ✔ | ✔ |
| `era5_temp_c` | Температура ERA5, °C | ✔ | ✔ |
| `era5_precip_mm` | Осадки ERA5, мм | ✔ | ✔ |
| `year` | Год | ✔ | ✔ |
| `primary_ndvi` | **Целевая переменная** — согласованный ряд NDVI | ✔ | ✔ (скрыт в контрольных точках) |
| `doy` | День года (day-of-year) | ✔ | ✔ |
| `ndvi_climatology_mean` | Среднее климатической нормы для полигона и фазы сезона | ✔ | ✔ |
| `ndvi_climatology_std` | Стандартное отклонение климатической нормы | ✔ | ✔ |
| `ndvi_zscore` | Z-score `primary_ndvi` относительно нормы | ✔ | — |
| `n_reference_years` | Число лет, по которым посчитана норма | ✔ | ✔ |
| `status` | Статус аномалии по Z-score | ✔ | — |
| `is_synthetic_gap` | Признак контрольной точки | — | ✔ |
| `crop_type` | Тип культуры (статичный признак полигона) | ✔ | ✔ |

Итого: 21 колонка в train, 20 в test.

### Сводка train vs test

| Показатель | Train | Test |
|---|---|---|
| Строк | 99 955 | 57 185 |
| Полигонов | 39 | 78 (все 39 из train + 39 новых, которых в train нет) |
| Период | 2010-04-01 … 2024-10-30 | 2010-04-01 … 2025-10-30 |
| Сезонное окно | апрель–октябрь, `doy` 91–304 | то же |
| Строк на пару полигон-год | 213 (ежедневная сетка) | 213 |
| `primary_ndvi` заполнен | 30 520 строк | 17 641 строка |
| `s2_ndvi` заполнен | 11 235 | 7 815 |
| `landsat_ndvi` заполнен | 15 592 | 8 243 |
| `modis_ndvi` заполнен | 7 527 | 3 957 |
| `era5_temp_c` заполнен | 92 655 | 48 002 |
| Диапазон `primary_ndvi` | −2.13 … 0.962 (есть отрицательные выбросы) | — |

Доля дней с заполненным `primary_ndvi` растёт по годам: около 18 % в 2010 году и 40–44 % в 2019–2024 годах.

### Распределение по культурам (`crop_type`)

| Культура | Train, строк | Test, строк |
|---|---|---|
| озимая пшеница | 52 787 | 32 160 |
| зерновые | 27 119 | 16 209 |
| подсолнечник | 15 975 | 1 278 |
| пастбища/зерновые | 4 074 | 7 538 |

### Климатическая норма (`n_reference_years`)

Почти всегда либо `0` (нормы нет, `ndvi_climatology_*` пусты), либо `15`. Промежуточные значения 7–14 встречаются редко (единицы и десятки строк).

### Статусы аномалий в train (`status`)

| Статус | Строк |
|---|---|
| Штатное развитие | 25 661 |
| Угнетение биомассы | 4 352 |
| Критическая аномалия | 507 |
| пусто | 69 435 |

Проверено по данным: `status` детерминированно соответствует порогам `ndvi_zscore` из [теоретического раздела](02-theory-and-glossary.md): `Z ≥ −1` → штатное, `−2 ≤ Z < −1` → угнетение, `Z < −2` → критическая. `ndvi_zscore` заполнен ровно в тех же строках, что и `primary_ndvi`.

### Контрольные точки в test (`is_synthetic_gap = True`)

| Показатель | Значение |
|---|---|
| Всего контрольных строк | **3 112** |
| Полигонов с контрольными точками | 78 (все) |
| На один полигон | от 4 до 148 |

Распределение по годам:

| Год | 2010 | 2011 | 2012 | 2013 | 2014 | 2015 | 2016 | 2017 | 2018 | 2019 | 2020 | 2021 | 2022 | 2023 | 2024 | 2025 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| Строк | 81 | 96 | 73 | 108 | 107 | 102 | 123 | 155 | 123 | 218 | 215 | 171 | 195 | 203 | 217 | **925** |

Почти 30 % контрольных точек приходится на 2025 год, которого нет в train.

По культурам: озимая пшеница — 2 030, зерновые — 678, пастбища/зерновые — 322, подсолнечник — 82.

> **Важно.** В строках с `is_synthetic_gap = True` заполнены только `anon_polygon_id`, `date`, `is_synthetic_gap` и `crop_type`. Все остальные колонки пустые, включая `year`, `doy`, `era5_temp_c`, `era5_precip_mm`, `ndvi_climatology_mean`, `ndvi_climatology_std` и `n_reference_years`. Их нужно восстанавливать из `date` и соседних строк того же полигона. Из-за `NaN` колонки `year`, `doy` и `n_reference_years` в test читаются pandas как `float`.

### Практические следствия для пайплайна

- Календарные признаки (`year`, `doy`) для контрольных точек вычислять из `date`, а не брать из файла.
- Метеоданные ERA5 в контрольной строке замаскированы, но доступны в соседних днях того же полигона — их можно интерполировать или брать окном.
- Климатическую норму можно пересчитать самостоятельно по паре `polygon + doy` из строк с известным `primary_ndvi` в train и test, не полагаясь на замаскированные `ndvi_climatology_*`.
- Валидацию строить с hold-out по полигонам (в test 39 полигонов, которых нет в train) и по сезонам (2025 год есть только в test).
- Синтетические пропуски для локальной валидации создавать из строк с известным `primary_ndvi`, маскируя те же колонки, что замаскированы в test.
- Перед загрузкой проверять `submission.csv` локально: ровно 3 112 строк, уникальные пары `anon_polygon_id + date`, без `NaN`.
