# Дополнения к анализу поля

Сохраняем выбор полей, модель и основной график NDVI. Ветка: `feat/field-insights`.

1. **Карта снимка.** Сворачиваемая панель для географических полей: NDVI, NDMI и изменение NDVI относительно предыдущего пригодного снимка. Sentinel-2 L2A, сетка 20 м, облака/тени/снег исключаются по SCL. Показываем дату, чистую площадь, среднее и легенду. Изменения считаются только на общих чистых пикселях; отдельно указываем площадь сравнения. Карта помогает выбрать место осмотра, не диагностирует причину.
2. **NDMI.** (NIR − SWIR1) / (NIR + SWIR1), среднее чистых пикселей. Дополнительная вкладка существующего графика, без интерполяции облачных дней. Исторический диапазон допускается только при наличии минимум трёх предыдущих загруженных сезонов; при одном сезоне показываем только наблюдения. NDWI из кейса не переименовываем.
3. **Погодные события.** Отметки дней Tmax ≥ 30 °C и Tmin < 0 °C. Это явно подписанные погодные пороги для просмотра, не пороги повреждения культуры. При отсутствии Tmin/Tmax отметок нет. Кликом можно приблизить период.
4. **Почва.** ERA5-Land через Open-Meteo: средняя модельная влажность слоя 0–100 см, взвешенная по толщине слоёв 7/21/72 см. Только полные сутки (24 значения каждого слоя), затем сравнение с десятью предыдущими годами на ту же календарную дату. Единицы м³/м³. Сетка порядка 10 км, не датчик поля и не рекомендация полива.
5. **Загрузка.** Снимки загружаются по запросу, кэшируются на диске; повторное открытие не требует сбора. Обработка снимков в отдельном процессе, статус и повтор после ошибки в интерфейсе. Анонимные AOI без координат продолжают показывать доступные ряды.

Проверки: маски/отверстия полигона, формулы и общая площадь сравнения, пропуски погоды, API, E2E на существующем географическом поле и кейсе, разные годы, ошибки/повтор, мобильный экран. Реальные внешние источники проверяются отдельно от воспроизводимых тестов расчёта.

## Реализованный срез: площадь и динамика отклонения

Под основным графиком добавлены две компактные раскрываемые карточки.

- **Площадь снижения NDVI:** число общих чистых пикселей двух соседних пригодных снимков с ΔNDVI ≤ −0,10, умноженное на 0,04 га. Доля считается от площади, видимой на **обоих** снимках. Отдельно указано, какая часть всего контура сравнивалась. Сравнение показывается при общей чистой площади от 60%. Первый снимок и недостаточное покрытие дают «Нужны два снимка», не 0%. Это фильтр для осмотра, а не доказательство повреждения.
- **Динамика отклонения:** для последних 28 календарных дней, заканчивающихся последним пригодным наблюдением, сравниваем медианы Z наблюдений в двух окнах по 14 дней. Z берётся из гармонизированного наблюдения и ориентира на его дату. Минимум 2 разные даты с нормой и σ в каждом окне; одна дата учитывается один раз, приоритет S2 → Landsat → MODIS. Артефакты и восстановления не учитываются. ΔZ ≤ −0,5 — усиливается; ΔZ ≥ 0,5 — ослабевает; иначе без заметных изменений. Если обе медианы ≥ −1 — без снижения к ориентиру. Если при заметном ослаблении новая медиана ≥ −1 — вернулось к ориентиру. При недостатке данных вывод не делается. Это описательная эвристика, не изменение порогов детектора.
- **Карта:** реальный POST сбора, повтор после ошибки, сохранение кэша, выбор даты, слои NDVI/NDMI/изменение, контур, легенда. Старые ответы после смены поля/года не показываются. Анонимным AOI карта и площадь не предлагаются.

Погодные события, отдельный график NDMI и влажность почвы из пунктов выше остаются в плане; в этот срез не входят.

Автоматические проверки: `tests/test_insights.py`, `tests/test_service.py`, `tests/e2e/field_insights.js`. Погодные регрессии: `tests/e2e/weather_charts.js`.

Проверено 05.09.2026: 23 E2E-проверки новых карточек и карты, 34 погодных E2E-проверки, 38 backend-проверок (insights/service/weather). Для «Поля у Ростова» реально собраны 34 пригодные сцены 2025 года. В паре 18–28 октября 4178 общих пикселей, снижение от 0,1 у 22 пикселей: 0,88 га / 0,5266% сравниваемых 167,12 га. Числа сверены с сохранёнными `.npz`. Скриншоты: `artifacts/e2e/insights-desktop.png`, `insights-mobile.png`.

Источники:
- https://www.usgs.gov/landsat-missions/normalized-difference-moisture-index
- https://help.onesoil.ai/en/articles/8354961-checking-field-status-ndvi-and-weather-data
- https://open-meteo.com/en/docs/historical-weather-api
- https://climate.copernicus.eu/esotc/2025/soil-moisture
- https://extension.umn.edu/agriculture/crop-production/corn/dry-conditions-during-corn-pollination
