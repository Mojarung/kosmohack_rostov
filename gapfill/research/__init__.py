"""Эксперименты улучшенной модели (exp-008): признаки, обучение, калибровка и проверенные гипотезы.

Здесь лежит исследовательская часть задачи 1 — код, которым подбирались признаки, схемы маскирования
и калибровка по опубликованным историческим агрегатам. Четыре модуля используются и в конкурсном
инференсе (`gapfill.predict_improved`): `data` (признаки), `nn` (остаточная сеть), `calibrate`
и `constraints` (калибровка). Остальные — проверенные гипотезы, оставленные ради воспроизводимости
журнала экспериментов; в инференсе они не участвуют.

Запуск любого модуля — как обычно: `uv run python -m gapfill.research.train --help`.
"""
